name: User Interface Build and Test

on:
  workflow_dispatch:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./desktop
    steps:
      - name: checkout
        uses: actions/checkout@v6

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: "latest"

      - name: install dependencies
        run: yarn install --frozen-lockfile

      - name: lint
        run: yarn lint:ci

      - name: format
        run: yarn format:check

      - name: types
        run: yarn types:check

      - name: build
        run: yarn build
